"use strict";

//Showing Spelling mistake
let hasDriversLicence = false;
let passTest = true;

if (passTest) hasDriversLicence = true;
if (hasDriversLicence) console.log("i can drive 🚗🚗🚗");

// Notify the Reserve Words

const interface = "Audio"; // interface is Reserve Words
